This shows what can happen if you increment the same `int` variable with more than one thread.

After changing to this directory, you can run this program in your terminal with the following command.

```sh
java increment.java
```
